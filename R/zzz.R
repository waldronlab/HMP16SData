.onLoad <- function(libname, pkgname) {
    # fl <- system.file("extdata", "metadata.csv", package = "HMP16SData")
    # titles <- read.csv(fl, stringsAsFactors=FALSE)$Title
    # createHubAccessors(pkgname, titles)
}
